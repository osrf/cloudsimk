#!/usr/bin/env bash

cd /home/ubuntu/code/src_cloud_simulator

#
# update repo
#
# can't do that on a private repo
# hg revert -a
# hg pull -u

# execute script (its not there yet)
# ./first_aws_boot.bash

echo "done" > start_src_done.txt

